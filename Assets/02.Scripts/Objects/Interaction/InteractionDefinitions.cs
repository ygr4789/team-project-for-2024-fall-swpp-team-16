// 상호작용 등록 모듈
public static class InteractionDefinitions
{
    static InteractionDefinitions()
    {
        // A와 B의 상호작용 정의
        // InteractionRegistry.Register<A, B>(ABInteraction);
    }

    // A와 B의 상호작용 예시
    // private static void ABInteraction(Interactable a, Interactable b)
    // {
    //     Debug.Log($"{a.GetType().Name} interacts with {b.GetType().Name} in a detailed and complex way.");
    // }
}